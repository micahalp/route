{
  "name": "route-contract-audit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "forge test --no-match-path \"*Fork*\" --summary",
    "build": "forge build",
    "verify:snapshot": "node scripts/verify-snapshot.mjs"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.6.1"
  }
}
